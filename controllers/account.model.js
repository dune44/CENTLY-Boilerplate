const accountSchema = require('./../schema/account.schema');
const db = require('./db');

const accountModel = {
    Create: {
        account: (account) => {

        }
    },
    Read: {
    },
    Update: {

    },
    Delete: {
        
    }
};

module.exports = accountModel;
